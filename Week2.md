# Python 2주차 프로젝트 과제

이번 주 목표는 **데이터를 자세히 살펴보고 데이터 명세서를 작성하는 것**입니다.  
단순히 함수를 실행하는 데서 끝내지 말고, 각 컬럼이 분석에서 어떤 의미를 갖는지 정리하세요.

**필수:** 본인의 노트북에서 진행한 내용과 실행 결과를 스크린샷으로 첨부해주세요.  
👀 수행 인증샷은 필수입니다.

노트북은 반드시 위에서 아래로 순서대로 실행해주세요. 중간 셀만 실행하면 이전에 만든 변수가 없어 오류가 날 수 있습니다.

---

## 참고 교재

- 『파이썬 라이브러리를 활용한 데이터 분석』
  - 5장 판다스 시작하기: p.181~246
  - 6장 데이터 로딩과 저장, 파일 형식: p.247~309

---

## 이번 주 과제 목차

| 구분 | 내용 |
| --- | --- |
| 필수 1 | 데이터 불러오기 |
| 필수 2 | 기본 구조 확인 |
| 필수 3 | 컬럼 명세서 작성 |
| 필수 4 | 분석 활용 컬럼 선정 |
| 필수 5 | 데이터 이해 메모 |
| 선택 | 데이터 품질 메모 추가 |

---

<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 필수 과제

## 1. 데이터 불러오기

1주차에 선택한 데이터셋을 불러오세요. 파일 경로는 본인 폴더 구조에 맞게 조정하면 됩니다.

```python
import pandas as pd

df = pd.read_csv("data/서울시 지하철호선별 역별 승하차 인원 정보.csv", encoding="cp949")
```

## 2. 기본 구조 확인

아래 코드를 실행해 데이터의 크기, 앞부분, 컬럼명, 데이터 타입을 확인하세요.

```python
print(df.head())
print(df.shape)
print(df.info())
print(df.describe())
print(df.columns)
```

## 3. 컬럼 명세서 작성

주요 컬럼 8개 이상을 골라 명세서를 작성하세요. 모든 컬럼을 설명할 수 있다면 더 좋습니다.

| 컬럼명 | 데이터 타입 | 의미 | 예시 값 | 분석에서 사용할 가능성 |
| --- | --- | --- | --- | --- |
| 사용일자 | object | 승하차가 집계된 날짜(YYYYMMDD). 이 파일은 20260727~20260904 | 20260815 | 높음 |
| 호선명 | object | 노선 이름. 27종. 같은 역도 노선별로 행이 나뉨 | 2호선 | 중간 |
| 역명 | object | 지하철역 이름. 531종(노선 분리 포함). 환승역은 한 날짜에 여러 행 | 시청 | 높음 |
| 승차총승객수 | int64 | 그 날짜·노선·역에서 승차한 총 인원 | 47209 | 높음 |
| 하차총승객수 | int64 | 그 날짜·노선·역에서 하차한 총 인원 | 42448 | 높음 |
| 등록일자 | object | 데이터가 등록된 날짜. 사용일자보다 약 3일 뒤 | 20260907 | 낮음 |
| 날짜(파생) | datetime64 | 사용일자를 날짜형으로 변환한 값 | 2026-08-15 | 높음 |
| 요일(파생) | object | 날짜에서 뽑은 요일 | Saturday | 높음 |

## 4. 분석 활용 컬럼 선정

1주차 분석 질문에 답하기 위해 꼭 필요해 보이는 컬럼을 정리하세요.

```md
핵심 컬럼: 사용일자, 역명, 승차총승객수, 하차총승객수
보조 컬럼: 호선명, (파생) 날짜·요일
사용하지 않을 것 같은 컬럼: 등록일자
제외 이유: 데이터가 시스템에 등록된 행정 처리일일 뿐, 승객 이동과는 무관하다.
```

## 5. 데이터 이해 메모

데이터를 처음 살펴보고 알게 된 점을 정리하세요. 아직 분석 결과가 아니어도 괜찮습니다.

```md
관찰한 점 1:
  1행 단위가 '날짜 × 노선 × 역' 이라 역명만으로는 유일하지 않다.
  예) 서울역은 하루에 1·4호선, 경의선, 공항철도, 경부선으로 5개 행이 나온다.
  역 단위로 보려면 노선을 합산해야 한다.

관찰한 점 2:
  역마다 주중/주말 패턴이 크게 다르다.
  여의도 평균 승차는 주중 33,984 → 주말 13,111 로 줄지만, 홍대입구는 주중 30,390 ≈ 주말 32,886 이다.
  나중에 행사일과 비교할 때 '같은 요일'끼리 비교해야 한다.

분석 전에 더 확인해야 할 점:
  - 이 파일은 40일(7/27~9/4)치뿐이라, 특정 행사 분석을 하려면 그 기간 데이터를 더 받아야 한다.
  - 행사 날짜·장소 정보는 이 파일에 없으므로 따로 정리해서 붙여야 한다.
  - 일 단위 집계라 시간대별 혼잡은 볼 수 없다.
```

---

# 2️⃣ 선택 과제

데이터 품질을 미리 점검해보세요. 결측치나 이상해 보이는 값이 있는지 간단히 메모하면 3주차 전처리 계획을 세우기 쉬워집니다.

```python
print(df.isna().sum())
print(df.nunique())
```

```md
결측치가 있어 보이는 컬럼:
  없음. isna() 합계가 모든 컬럼 0 이다.

값의 종류가 너무 많거나 적은 컬럼:
  - 역명 531종: 노선 분리 집계라 실제 역 수보다 많다.
  - 사용일자 40종: 추가 데이터 필요

품질 확인이 더 필요한 컬럼:
  - 하차총승객수: 값이 0인 행이 167개 있다(충무로, 연신내, 복정 등). 승차는 모두 1 이상이라 집계 방식 문제로 보인다.
  - 호선명: 경부선·경인선 같은 광역철도 구간이 1호선과 어떻게 나뉘는지 확인이 필요하다.
```

---

# 3️⃣ 제출 체크리스트

- ✅ 데이터를 불러왔다.
- ✅ `head()`, `shape`, `info()`, `describe()` 결과를 첨부했다.
- ✅ 주요 컬럼 8개 이상에 대한 명세서를 작성했다.
- ✅ 분석에 사용할 핵심 컬럼을 선정했다.
- ✅ 데이터 이해 메모를 작성했다.

🎉 수고하셨습니다.  
다음 주에는 데이터의 결측치, 중복, 이상치를 점검하고 전처리 기준을 세웁니다.
