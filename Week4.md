# Python 4주차 프로젝트 과제

이번 주 목표는 **탐색적 데이터 분석(EDA)을 통해 데이터의 특징을 파악하는 것**입니다.  
분포, 빈도, 변수 간 관계를 살펴보며 중간 인사이트를 정리하세요.

**필수:** 본인의 노트북에서 진행한 내용과 실행 결과를 스크린샷으로 첨부해주세요.  
👀 수행 인증샷은 필수입니다.

노트북은 반드시 위에서 아래로 순서대로 실행해주세요. 중간 셀만 실행하면 이전에 만든 변수가 없어 오류가 날 수 있습니다.

---

## 참고 교재

- 『파이썬 라이브러리를 활용한 데이터 분석』
  - 5장 판다스 시작하기: p.181~246
  - 6장 데이터 로딩과 저장, 파일 형식: p.247~309
  - 9장 그래프와 시각화: p.381~465

---

## 이번 주 과제 목차

| 구분 | 내용 |
| --- | --- |
| 필수 1 | 단일 변수 분석 |
| 필수 2 | 범주형 변수 분석 |
| 필수 3 | 변수 관계 분석 |
| 필수 4 | 중간 인사이트 정리 |
| 선택 | 추가 EDA |

---

<br>

<!-- 여기까진 그대로 둬 주세요-->

---

# 1️⃣ 필수 과제

먼저 지난주에 저장한 정제 데이터를 불러오세요. 저장 파일명이 다르다면 본인의 파일명에 맞게 바꿔주세요.

```python
import pandas as pd

df_clean = pd.read_csv("data/cleaned_지하철_시간대별_승하차.csv")
```

## 1. 단일 변수 분석

숫자형 컬럼 1개 이상을 골라 분포를 확인하세요. 평균, 중앙값, 최댓값, 최솟값 등을 함께 보면 좋습니다.

```python
print(df_clean["18시-19시 승차인원"].describe())
df_clean["18시-19시 승차인원"].plot(kind="hist")
```

```md
평균: 35126명 / 중앙값: 19021명 / 최솟값: 0명 / 최댓값: 448390명 (표준편차 47179)

평균이 중앙값의 1.8배에 달할 만큼 오른쪽으로 심하게 치우친 분포다. 절반 이상의 역·노선·월 조합은
19021명 이하인데, 소수의 대형 환승역(3주차에 확인한 강남역 등)이 40만명대 값을 만들어 평균을
끌어올린다. 그래프에서도 대부분 값이 왼쪽에 몰려 있고 오른쪽으로 긴 꼬리가 이어지는 형태다.
```

## 2. 범주형 변수 분석

범주형 컬럼 1개 이상을 골라 값의 빈도를 확인하세요.

```python
print(df_clean["호선명"].value_counts())
print(df_clean["호선명"].value_counts(normalize=True))
```

```md
행 개수 기준 1위는 5호선(7484행, 8.95%), 꼴찌는 서해선(114행, 0.14%)이다.

다만 이 순위는 실제 승객이 많고 적음이 아니라 "노선당 역 수 × 관측된 월 수"의 차이일 가능성이
크다. 확인해보니 5호선은 67개 역 × 140개월치가 쌓여 있는 반면, 서해선은 3개 역 × 38개월치뿐이었다
(서해선처럼 최근 개통된 노선은 역도 적고 데이터 기간도 짧다). 값의 개수만으로 노선 인기를 판단하면
안 되고, 노선당 역 수·개통 시기를 같이 봐야 한다는 걸 알게 됐다.
```

## 3. 변수 관계 분석

분석 질문과 관련 있는 컬럼 2개를 골라 관계를 확인하세요. 숫자 컬럼끼리는 산점도로 보고, 범주형 컬럼과 숫자형 컬럼은 박스플롯으로 비교할 수 있습니다.

```python
print(df_clean["18시-19시 승차인원"].corr(df_clean["18시-19시 하차인원"]))
df_clean.plot(x="18시-19시 승차인원", y="18시-19시 하차인원", kind="scatter")

top8_lines = df_clean["호선명"].value_counts().head(8).index
df_clean[df_clean["호선명"].isin(top8_lines)].boxplot(column="18시-19시 승차인원", by="호선명")
```

```md
승차-하차 상관계수는 0.60으로, 양의 상관은 있지만 완전히 비례하지는 않는다. 즉 같은 시간대에
승차는 많은데 하차는 적은 역(반대의 경우도)이 있다는 뜻이라 개별 역 단위로 더 봐야 한다.

노선별로는 2호선(평균 98539명)과 3호선(평균 51868명)이 확실히 높고, 경원선(평균 12478명)이
가장 낮다. 도심 순환·환승역이 많은 노선일수록 저녁 승차 인원이 크게 높은 것으로 보인다.
```

## 4. 중간 인사이트 정리

EDA 결과를 바탕으로 지금까지 알게 된 점을 정리하세요.

```md
발견한 패턴 1:
저녁(18-19시) 승차인원 분포가 심하게 오른쪽으로 치우쳐 있다. 평균 35126명 vs 중앙값 19021명이고,
최댓값 448390명은 3주차에도 확인했던 강남역 같은 대형 환승역에서 나온다. 노선 전체를 하나의
숫자로 요약할 때는 평균보다 중앙값이 더 대표성이 있어 보인다.

발견한 패턴 2:
노선별 저녁 승차 평균 차이가 매우 크다. 2호선·3호선은 5만~10만명대인데 경원선·6호선 등은
1만~2만명대에 그친다. 다만 호선명 값 개수 차이(역 수·개통 시기)가 섞여 있어서, 다음 주에는
노선별이 아니라 역 단위로 다시 비교해봐야 공정한 비교가 될 것 같다.

추가로 확인하고 싶은 점:
승차-하차 상관이 0.60로 완전한 비례가 아니므로, 승차는 많고 하차는 적은 역(또는 반대)이 구체적으로
어디인지 봐야 한다. 이런 역이 행사장 인근일 가능성이 있어 다음 주 행사 데이터 결합 때 참고할 만하다.

분석 질문에 대한 중간 답변:
아직 행사 일정 데이터가 결합되지 않아 "행사가 지하철 수요에 미치는 영향"을 직접 확인할 수는 없다.
다만 이번 주에 노선·시간대별 기본 수요 패턴(어떤 노선이 원래 붐비는지, 승하차 분포가 얼마나
치우쳐 있는지)을 확보했으므로, 다음 주 파생변수(요일, 피크 여부)와 행사 라벨을 붙이면 "평소보다
얼마나 늘었는지"를 비교할 기준선이 마련된 셈이다.
```

---

# 2️⃣ 선택 과제

분석 질문과 직접 연결되는 그래프를 하나 더 만들어보세요. 그래프 종류는 자유롭게 선택해도 됩니다.

```python
df_clean["호선명"].value_counts().head(10).plot(kind="bar")
```

---

# 3️⃣ 제출 체크리스트

✅ 숫자형 컬럼의 분포를 확인했다.
✅ 범주형 컬럼의 빈도와 비율을 확인했다.
✅ 변수 관계를 1개 이상 확인했다.
✅ EDA 결과를 바탕으로 중간 인사이트를 작성했다.

🎉 수고하셨습니다.  
다음 주에는 파생변수를 만들고, 그룹별 요약표를 통해 분석 질문에 더 구체적으로 답해봅니다.
